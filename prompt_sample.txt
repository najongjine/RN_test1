내 프로젝트는 react native expo 야.
app/utils/db_crud.ts
여기에 expo sqlite를 이용해서 데이터를 insert 하는 모듈 샘플을
만들어줘.
쌩쿼리로 하고싶어.
당연히 매개변수 받아서 insert 하는 샘플 쿼리여야해.

insert할 테이블 대상은
await db.execAsync(`
      CREATE TABLE IF NOT EXISTS t_testmemo (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        title TEXT NOT NULL CHECK (length(title) <= 500),
        content TEXT,
        created_dt TEXT DEFAULT (datetime('now', 'localtime')),
        updated_dt TEXT DEFAULT (datetime('now', 'localtime'))
      );
    `);

이거야


---------------------


db_crud.ts 여기에 나의 메모 데이터를 insert 시키는 
모듈이 있어.

MemoEditScreen.tsx 여기에서 저장 버튼을 부르면,
db_crud.ts 에 있는 insert 모듈을 갔다 써서 데이터를 저장 시키고,
홈 경로로 가게 해줘.



--------------------


db_crud.ts

이게 db 조작하는 모듈들이 모여있는 파일이야.

index.tsx
여기에 내가 작성한 메모들을 

제목, 내용 조금..., 날짜

이렇게 list 형태로 보고싶어.
(참고: db_crud.ts 여기에 메모리스트 가져오는 모듈이 없으면,
니가 만들고 작업을 수행해줘)


----------------

index.tsx 여기에서
메모 누르면 MemoEditScreen.tsx 가게 해줘.
그리고 props로 readonly 라는 데이터도 함께 보내게 해줘.


---------------

app/utils/db_crud.ts

여기에 
* t_testmemo table 에서 id 로 메모데이터 가져오기
* t_testmemo table 에서 id 로 메모데이터 수정하기
* t_testmemo table 에서 id 로 삭제하기

모듈들을 추가해줘